# Portfólio — Erick Kamizato Scilo

Página única, estática, sem build — HTML/CSS puro publicado com **GitHub Pages**.

## Publicado em

https://erickscilo.github.io/portfolio-erick/

O site é publicado automaticamente pelo GitHub Pages a partir da branch `main`. Todo `git push` atualiza o site sozinho, sem passo manual.

## Estrutura

Um único `index.html` com CSS inline. Sem dependências, sem processo de build — abrir o arquivo direto no navegador já funciona.
